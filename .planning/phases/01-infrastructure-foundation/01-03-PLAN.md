---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - deploy/README.md
autonomous: false
requirements: [INFRA-07]
user_setup:
  - service: VPS (andescode.com.ar) — PocketBase admin UI
    why: "Native backup cron is configured in the PocketBase admin settings UI and retention pruning runs on the VPS; both require access Claude does not have"
    dashboard_config:
      - task: "Configure automated backup cron + maxKeep in PocketBase admin settings"
        location: "https://andescode.com.ar/_/ → Settings → Backups"
must_haves:
  truths:
    - "PocketBase runs a daily automated backup using its native backup mechanism"
    - "Backups older than 7 days are pruned automatically (retention = 7)"
    - "A test certificate created in the admin UI is retrievable by ID with no auth token (public viewRule)"
    - "import.meta.env.VITE_POCKETBASE_URL resolves correctly in the browser"
    - "All five ROADMAP Phase 1 success criteria pass"
  artifacts:
    - path: "PocketBase Settings → Backups (cron + maxKeep)"
      provides: "daily backup with 7-day retention"
    - path: "/home/[user]/pocketbase/pb_data/backups/"
      provides: "backup archive output directory"
  key_links:
    - from: "PocketBase backup cron"
      to: "pb_data/backups/"
      via: "native backup API"
      pattern: "backups"
---

<objective>
Complete the Walking Skeleton and prove the phase gate. Configure PocketBase's native daily backup with 7-day retention (INFRA-07, D-11/D-12/D-13), then run the full end-to-end verification: create a test certificate, confirm it is publicly retrievable by ID with no auth (viewRule = "", ROADMAP criterion 3), and confirm `import.meta.env.VITE_POCKETBASE_URL` resolves in the browser (ROADMAP criterion 5). This closes the loop opened in Plan 01 (artifacts) and Plan 02 (deploy).

Not autonomous — backup config and the admin-UI test record require VPS / admin-UI access Claude does not have. Claude supplies exact settings and verification steps; human-action checkpoints execute and report results.

Purpose: Guarantee data durability (no feature phase can start without recoverable backups) and produce documented proof that all five ROADMAP success criteria pass.
Output: Backup cron configured with 7-day retention; a verified test certificate; `deploy/README.md` updated with the confirmed backup path and the full phase-gate results.
</objective>

<artifacts_this_phase_produces>
Artifacts created by this phase (exclude from drift verification):
- PocketBase backup cron schedule + `maxKeep=7` retention setting
- VPS backup output directory `/home/[user]/pocketbase/pb_data/backups/`
- One test certificate record (used for verification; may be left or removed)
- Phase-gate results section appended to `deploy/README.md`
</artifacts_this_phase_produces>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/AndesCode/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/AndesCode/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@deploy/README.md
@.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure native daily backup with 7-day retention</name>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (backup approach lines 64-66, Don't Hand-Roll backup line 357, Assumption A2 backup path lines 480, native backup v0.16+ line 52)
    - .planning/phases/01-infrastructure-foundation/01-CONTEXT.md (D-11 local disk, D-12 7-day retention, D-13 native mechanism)
    - deploy/README.md (current runbook)
  </read_first>
  <what-built>
    Claude provides the exact PocketBase admin-UI settings (cron expression + maxKeep) and the path to verify. The human applies them in the admin UI and confirms the backup output path. Claude cannot access the admin UI.
  </what-built>
  <how-to-verify>
    In the PocketBase admin UI at `https://andescode.com.ar/_/` → Settings → Backups:
    1. Enable automated backups; set the cron expression to a nightly schedule (`0 2 * * *` — 2am, Claude's discretion per CONTEXT; standard nightly window).
    2. Set `maxKeep` to `7` (D-12 — PocketBase prunes backups beyond the 7 most recent; with a daily cron this is 7-day retention via the native mechanism, D-13 — no custom shell prune script).
    3. Save settings.
    4. Trigger a manual backup once via the "Backup now" button (or `POST /api/backups` with the superuser token) to confirm the mechanism works and to discover the actual output path.
    5. On the VPS: `ls -la /home/[user]/pocketbase/pb_data/backups/` — confirm a `.zip` backup archive appears (verifies Assumption A2 path; if the real path differs, record it). Confirm the directory/file is not world-readable (chmod restricted per RESEARCH security threat).
    Paste back: the cron expression, maxKeep value, and the `ls` output of the backups directory.
  </how-to-verify>
  <action>
    Claude supplies the resolved username in the path and the exact cron + maxKeep values. After the human confirms, Claude records the ACTUAL verified backup path in `deploy/README.md` (resolving RESEARCH Assumption A2 — the default path may differ from `pb_data/backups/`). Confirm maxKeep=7 satisfies D-12 retention via the native mechanism (no custom prune cron needed). If the manual backup produces no file, diagnose: backups may be disabled or the superuser token lacks permission.
  </action>
  <acceptance_criteria>
    - PocketBase Settings → Backups shows automated backups enabled with a nightly cron and `maxKeep = 7`
    - A manual backup produced a `.zip` archive in the backups directory
    - The actual backup output path is recorded in `deploy/README.md`
    - Backup uses PocketBase's native mechanism (no custom `cp`/`tar` shell script — D-13)
  </acceptance_criteria>
  <resume-signal>Paste the cron expression, maxKeep value, and backups-directory ls output; then type "backup-set"</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify public viewRule and full phase gate</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria, all 5 — lines 27-32)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Validation Architecture lines 524-559, viewRule public-by-ID lines 542)
    - deploy/README.md (phase-gate curl commands)
  </read_first>
  <what-built>
    Plan 01 wired the schema (viewRule = "") and the frontend env var; Plan 02 made the API reachable over HTTPS. This checkpoint creates a real test certificate and proves the two remaining ROADMAP criteria (3: public view-by-ID, 5: browser env var), then re-runs all five criteria as the final phase gate.
  </what-built>
  <how-to-verify>
    1. In the PocketBase admin UI → Collections → certificates → New record: create a test certificate with `certificateCode = AC-2025-TEST`, status `active`, and all required fields filled. Save. Copy its record `id`.
    2. From any machine WITHOUT an auth token:
       `curl -s https://andescode.com.ar/api/collections/certificates/records/<record-id>` → expect 200 with the certificate JSON (viewRule = "" allows public single-record fetch — ROADMAP criterion 3).
    3. Re-run the full phase gate (ROADMAP criteria 1, 2, 4):
       - `.../records/nonexistent` → 404
       - `.../records` (no auth) → 403
       - `http://andescode.com.ar:8090/api/health` → timeout/refused
    4. Criterion 5 (browser env var): with the SPA deployed, open https://andescode.com.ar in a browser, open DevTools Console, type `import.meta.env.VITE_POCKETBASE_URL` → expect `"https://andescode.com.ar"`. (If the SPA build predates Plan 01's env files, rebuild and redeploy the SPA first so the env var is baked into the bundle.)
    Paste back all results.
  </how-to-verify>
  <action>
    Claude provides the exact curl for the resolved record id and the DevTools snippet. Record every result in a "Phase 1 Gate Results" section appended to `deploy/README.md`. If criterion 3 returns 403/404 instead of 200, diagnose: viewRule may not be `""` (re-check the applied migration) or the record id/code is wrong. If criterion 5 returns `undefined`, the SPA bundle was built before the `.env` files existed — rebuild (`npm run build`) and redeploy the SPA, then retest.
  </action>
  <acceptance_criteria>
    - GET `/api/collections/certificates/records/<test-id>` with no auth returns 200 + certificate JSON (criterion 3)
    - `/api/collections/certificates/records/nonexistent` returns 404 (criterion 1)
    - `/api/collections/certificates/records` no-auth returns 403 (criterion 2)
    - port 8090 external request times out / refused (criterion 4)
    - browser `import.meta.env.VITE_POCKETBASE_URL` equals `"https://andescode.com.ar"` (criterion 5)
    - All five results recorded in `deploy/README.md`
  </acceptance_criteria>
  <resume-signal>Paste the five phase-gate results (200 view, 404, 403, timeout, env-var value); then type "phase-gate-pass" or describe failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/collections/certificates/records/:id | viewRule = "" intentionally allows unauthenticated single-record reads by ID |
| VPS filesystem → backup archives | Backups contain full DB (DNI data); must not be world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | backup archives in pb_data/backups/ | mitigate | Store under `/home/[user]/pocketbase/` (home dir, not web-served); Task 1 confirms restricted permissions; backups never proxied by nginx |
| T-01-09 | Denial of Service | data loss without backups | mitigate | Native daily backup + maxKeep=7 retention ensures recoverable state (INFRA-07); manual backup tested in Task 1 |
| T-01-10 | Information Disclosure | public view-by-ID enumeration | accept | viewRule = "" is by design — verification requires knowing the exact certificateCode/id; list is blocked (403); public name-search is out of scope per REQUIREMENTS |
</threat_model>

<verification>
- Backups: automated cron enabled, maxKeep=7, manual backup produced a .zip
- Phase gate: all five ROADMAP criteria pass and are recorded in deploy/README.md
- Public viewRule confirmed: 200 by-id with no auth, 403 on list
</verification>

<success_criteria>
- INFRA-07: daily native backup with 7-day retention configured and tested
- All five ROADMAP Phase 1 success criteria pass
- Walking Skeleton complete: PocketBase reachable over HTTPS, schema live, env var resolving, data backed up
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md` when done
</output>
