---
phase: 01-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vite-env.d.ts
  - .env
  - .env.production
  - .gitignore
  - pb_migrations/1780790669_create_certificates.js
  - deploy/ecosystem.config.js
  - deploy/README.md
autonomous: true
requirements: [INFRA-04, INFRA-05, INFRA-06, INFRA-08]
must_haves:
  truths:
    - "import.meta.env.VITE_POCKETBASE_URL resolves to the VPS URL in a Vite build"
    - "The certificates migration declares all 14 schema fields with the exact API rules from D-10"
    - "certificateCode has a UNIQUE index in the migration"
    - "TypeScript build (tsc) passes with the augmented vite-env.d.ts"
  artifacts:
    - path: "pb_migrations/1780790669_create_certificates.js"
      provides: "certificates collection schema + API rules + unique index"
      contains: "name: \"certificates\""
    - path: ".env"
      provides: "VITE_POCKETBASE_URL for dev"
      contains: "VITE_POCKETBASE_URL="
    - path: ".env.production"
      provides: "VITE_POCKETBASE_URL for prod build"
      contains: "VITE_POCKETBASE_URL="
    - path: "src/vite-env.d.ts"
      provides: "ImportMetaEnv type augmentation"
      contains: "VITE_POCKETBASE_URL"
    - path: "deploy/ecosystem.config.js"
      provides: "PM2 config for PocketBase binary (versioned in repo)"
      contains: "interpreter"
  key_links:
    - from: "pb_migrations/1780790669_create_certificates.js"
      to: "certificates.listRule"
      via: "migration Collection constructor"
      pattern: "@request\\.auth\\.id"
    - from: "src/vite-env.d.ts"
      to: "import.meta.env.VITE_POCKETBASE_URL"
      via: "ImportMetaEnv interface"
      pattern: "VITE_POCKETBASE_URL"
---

<objective>
Produce every repo-side artifact the infrastructure depends on: the versioned PocketBase migration that creates the `certificates` collection (schema + API rules + unique index), the Vite environment variable wiring (`.env`, `.env.production`, type augmentation), the `.gitignore` rule for `.env.local`, and the versioned PM2 `ecosystem.config.js` (with a deploy README) that Plan 02 copies to the VPS.

This is the first slice of the Walking Skeleton: it does not touch the VPS, but it creates the source-of-truth files that Plan 02 deploys and Plan 03 verifies. All work is autonomous and verifiable with `tsc` and grep — no SSH required.

Purpose: Establish reproducible, git-versioned infrastructure-as-code (D-08) so the VPS deploy in Plan 02 is a mechanical copy + run, not improvisation.
Output: `pb_migrations/1780790669_create_certificates.js`, `.env`, `.env.production`, augmented `src/vite-env.d.ts`, updated `.gitignore`, `deploy/ecosystem.config.js`, `deploy/README.md`.
</objective>

<artifacts_this_phase_produces>
Symbols and files created by this phase (exclude from drift verification):
- File `pb_migrations/1780790669_create_certificates.js` — PocketBase JS migration
- PocketBase collection `certificates` with fields: `certificateCode`, `studentName`, `dni`, `university`, `degree`, `startDate`, `endDate`, `issueDate`, `score`, `technologies`, `competencies`, `description`, `supervisorName`, `status`
- DB index `idx_certificates_code` (UNIQUE on `certificateCode`)
- File `.env`, file `.env.production` with env var `VITE_POCKETBASE_URL`
- TypeScript interfaces `ImportMetaEnv` and `ImportMeta` (augmentation) in `src/vite-env.d.ts`
- File `deploy/ecosystem.config.js` — PM2 app definition named `pocketbase`
- File `deploy/README.md` — VPS deploy runbook
</artifacts_this_phase_produces>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/AndesCode/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/AndesCode/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the certificates collection migration</name>
  <files>pb_migrations/1780790669_create_certificates.js</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (full migration pattern, lines 100-156 — the canonical schema and field types)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Pattern 3 lines 243-309, Pitfall 1 listRule semantics lines 369-374, Pitfall 6 select-vs-json lines 404-409)
    - .planning/REQUIREMENTS.md (INFRA-04 field list line 13, INFRA-06 exact API rules line 15)
  </read_first>
  <action>
    Create the migration file at the exact path `pb_migrations/1780790669_create_certificates.js` (directory `pb_migrations/` does not exist — create it). Use the PocketBase JS migration format: a `migrate((app) => { ... }, (app) => { ... })` call where the up-callback constructs `new Collection({...})` and calls `app.save(collection)` (use the callback parameter `app`, NOT global `$app`), and the down-callback calls `app.findCollectionByNameOrId("certificates")` then `app.delete(collection)`.

    Collection config — set EXACTLY (per D-10 / INFRA-06):
    - `type: "base"`, `name: "certificates"`
    - `listRule: '@request.auth.id != ""'` (authenticated only — NEVER null, NEVER empty string; empty string would publicly expose all DNI data)
    - `viewRule: ""` (public single-record view — anyone verifies a cert by ID)
    - `createRule: null`, `updateRule: null`, `deleteRule: null` (superuser only)

    Fields array — all 14 fields from INFRA-04, in this order with these types:
    certificateCode (text, required), studentName (text, required), dni (text, required), university (text, required), degree (text, required), startDate (date, required), endDate (date, required), issueDate (date, required), score (number, NOT required), technologies (json, NOT required — use type "json" per RESEARCH open question A1 for free-form tags, NOT select), competencies (json, NOT required), description (text, NOT required), supervisorName (text, required), status (select, required, values ["active","revoked"], maxSelect 1).

    Indexes array (INFRA-05): exactly one entry, the string `CREATE UNIQUE INDEX idx_certificates_code ON certificates (certificateCode)`.

    Add a top-of-file comment block citing INFRA-04/05/06 and D-10, and inline comments on listRule/viewRule explaining the null-vs-empty-string distinction.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('pb_migrations/1780790669_create_certificates.js','utf8');const checks=[/name:\s*[\"']certificates[\"']/,/listRule:\s*['\"]@request\.auth\.id != /,/viewRule:\s*[\"']{2}\s*,/,/createRule:\s*null/,/updateRule:\s*null/,/deleteRule:\s*null/,/certificateCode/,/studentName/,/dni/,/university/,/degree/,/startDate/,/endDate/,/issueDate/,/score/,/technologies/,/competencies/,/description/,/supervisorName/,/status/,/CREATE UNIQUE INDEX idx_certificates_code/,/app\.save\(/,/app\.delete\(/];const fail=checks.filter(r=>!r.test(s));if(fail.length){console.error('MISSING:',fail.map(String));process.exit(1)}console.log('migration OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pb_migrations/1780790669_create_certificates.js` exists
    - File contains `name: "certificates"` and all 14 field names listed above
    - File contains `listRule: '@request.auth.id != ""'` (auth expression, not null, not empty)
    - File contains `viewRule: ""`, `createRule: null`, `updateRule: null`, `deleteRule: null`
    - File contains the literal `CREATE UNIQUE INDEX idx_certificates_code ON certificates (certificateCode)`
    - `technologies` and `competencies` use `type: "json"` (not `select`)
    - File contains both `app.save(` and `app.delete(` (up and down migration)
    - The verify node command exits 0
  </acceptance_criteria>
  <done>The migration declares the full certificates schema, the exact D-10 API rules, the unique index on certificateCode, and a reversible down migration.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Vite env vars and TypeScript augmentation</name>
  <files>.env, .env.production, src/vite-env.d.ts, .gitignore</files>
  <read_first>
    - src/vite-env.d.ts (current content — single line `/// <reference types="vite/client" />`)
    - .gitignore (current content — confirm `*.local` rule already present, lines covering `.local`)
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (vite-env.d.ts target content lines 32-43, .env content lines 59-72, .env.production lines 80-87)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Pattern 4 lines 311-340, Anti-pattern: no imports in vite-env.d.ts line 349)
  </read_first>
  <action>
    Augment `src/vite-env.d.ts` WITHOUT removing the existing `/// <reference types="vite/client" />` triple-slash directive (keep it as line 1). Append an `interface ImportMetaEnv { readonly VITE_POCKETBASE_URL: string }` and an `interface ImportMeta { readonly env: ImportMetaEnv }`. CRITICAL: do NOT add any `import` statement — any import breaks global module augmentation. Use `readonly` modifiers (satisfies strict mode `noUnusedLocals` without introducing locals).

    Create `.env` at repo root with a comment line plus `VITE_POCKETBASE_URL=https://andescode.com.ar` (D-09: dev points to the production VPS, no local PocketBase).

    Create `.env.production` at repo root with a comment line plus `VITE_POCKETBASE_URL=https://andescode.com.ar` (identical value per D-09; separate file maintains Vite convention).

    In `.gitignore`: the existing `*.local` rule already covers `.env.local`. Add an explicit comment-and-rule block under a `# Env` heading: ignore `.env.local` and `.env.*.local` explicitly, but DO NOT ignore `.env` or `.env.production` (they hold only the public VPS URL, no secrets, and must be committed for reproducible builds). If `*.local` already matches, still add the explicit `.env.local` line with a clarifying comment so intent is unambiguous.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5; node -e "const fs=require('fs');['.env','.env.production'].forEach(f=>{if(!/VITE_POCKETBASE_URL=https:\/\/andescode\.com\.ar/.test(fs.readFileSync(f,'utf8')))throw new Error('bad '+f)});const d=fs.readFileSync('src/vite-env.d.ts','utf8');if(!/VITE_POCKETBASE_URL/.test(d))throw new Error('no type aug');if(/^\s*import\s/m.test(d))throw new Error('import present — breaks augmentation');if(!/reference types=\"vite\/client\"/.test(d))throw new Error('lost vite client ref');console.log('env wiring OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc + vite build) exits 0
    - `.env` and `.env.production` each contain `VITE_POCKETBASE_URL=https://andescode.com.ar`
    - `src/vite-env.d.ts` line 1 still is `/// <reference types="vite/client" />`
    - `src/vite-env.d.ts` contains `interface ImportMetaEnv` and `readonly VITE_POCKETBASE_URL: string`
    - `src/vite-env.d.ts` contains NO `import` statement
    - `.gitignore` ignores `.env.local` but NOT `.env` or `.env.production`
    - `git check-ignore .env` returns nothing (not ignored); `git check-ignore .env.local` returns `.env.local`
  </acceptance_criteria>
  <done>The frontend can read `import.meta.env.VITE_POCKETBASE_URL` with full type safety; committed env files target the VPS; `.env.local` stays git-ignored.</done>
</task>

<task type="auto">
  <name>Task 3: Version the PM2 ecosystem config and deploy runbook in repo</name>
  <files>deploy/ecosystem.config.js, deploy/README.md</files>
  <read_first>
    - .planning/phases/01-infrastructure-foundation/01-PATTERNS.md (ecosystem.config.js pattern lines 165-184, nginx block lines 195-229)
    - .planning/phases/01-infrastructure-foundation/01-RESEARCH.md (Pattern 1 lines 171-195, Pitfall 5 migrationsDir lines 398-402, Pitfall 4 pm2 startup lines 390-394)
    - .planning/phases/01-infrastructure-foundation/01-CONTEXT.md (D-04 through D-07 PM2 decisions, D-05 path)
  </read_first>
  <action>
    Create `deploy/ecosystem.config.js` — a git-versioned source-of-truth copy of the PM2 config that Plan 02 will place on the VPS at `/home/[user]/pocketbase/ecosystem.config.js`. Export `module.exports = { apps: [{ ... }] }` with: `name: "pocketbase"`, `script: "/home/__USER__/pocketbase/pocketbase"`, `interpreter: "none"` (required — PocketBase is a binary, not a Node script), and `args: "serve --http=127.0.0.1:8090 --dir=/home/__USER__/pocketbase/pb_data --migrationsDir=/home/__USER__/pocketbase/pb_migrations"`. Use the literal placeholder token `__USER__` for the VPS username (resolved during the Plan 02 nginx audit, D-02) and add a header comment stating that `__USER__` must be replaced before deploy.

    The `--migrationsDir` flag is mandatory (RESEARCH Pitfall 5): PocketBase only scans `pb_migrations/` relative to its working directory, so the deploy must place the migration where this flag points.

    Create `deploy/README.md` — a VPS deploy runbook that Plan 02 follows. Include numbered sections (no shell execution here, just documented commands):
    1. nginx audit (`nginx -T`) to discover the SPA root path and the process user; replace `__USER__` everywhere.
    2. Download latest stable PocketBase binary to `/home/__USER__/pocketbase/`.
    3. Copy `deploy/ecosystem.config.js` (with `__USER__` resolved) and the repo `pb_migrations/` dir to `/home/__USER__/pocketbase/pb_migrations/`.
    4. Start via `pm2 start ecosystem.config.js`, then `pm2 save`, then `pm2 startup` (run the printed root command — Pitfall 4).
    5. The nginx reverse-proxy block to add (copy the full `/api/`, `/_/`, `/` blocks from 01-PATTERNS.md lines 195-229), with the rule that specific prefixes come BEFORE `location /`, and `proxy_buffering off` on `/api/`. Note the SPA `root` placeholder gets filled from the audit.
    6. Firewall: block public access to port 8090 (ufw or iptables).
    7. First-launch superuser creation via `/_/`.
    8. The four phase-gate curl verification commands from RESEARCH lines 531-549.
  </action>
  <verify>
    <automated>node -e "const m=require('./deploy/ecosystem.config.js');const a=m.apps[0];if(a.name!=='pocketbase')throw new Error('name');if(a.interpreter!=='none')throw new Error('interpreter must be none');if(!/--http=127\.0\.0\.1:8090/.test(a.args))throw new Error('http bind');if(!/--migrationsDir=/.test(a.args))throw new Error('migrationsDir missing');console.log('ecosystem OK')"; node -e "const fs=require('fs');const r=fs.readFileSync('deploy/README.md','utf8');['nginx -T','pm2 start','pm2 save','pm2 startup','proxy_buffering off','8090','location /api/','location /_/'].forEach(t=>{if(!r.includes(t))throw new Error('README missing: '+t)});console.log('README OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/ecosystem.config.js` requires cleanly and exposes `apps[0]` with `name: "pocketbase"`, `interpreter: "none"`
    - `args` contains `serve`, `--http=127.0.0.1:8090`, `--dir=`, and `--migrationsDir=`
    - `deploy/ecosystem.config.js` uses `__USER__` placeholder token with a replace-before-deploy comment
    - `deploy/README.md` documents: nginx audit, PocketBase download, pm2 start/save/startup, nginx `/api/` + `/_/` + `/` blocks with `proxy_buffering off`, port 8090 firewall block, superuser creation, and the curl phase-gate checks
    - both verify node commands exit 0
  </acceptance_criteria>
  <done>The PM2 config and a complete VPS deploy runbook are versioned in the repo, ready for Plan 02 to execute mechanically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → PocketBase REST | Untrusted public clients hit `/api/*`; API rules are the only access control |
| git repo → VPS | Committed env/config files become production config; secrets must not leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | certificates.listRule in migration | mitigate | Migration sets `listRule = '@request.auth.id != ""'` — never `""` (public DNI enumeration) and never `null` (breaks admin); verify gate greps for the auth expression |
| T-01-02 | Information Disclosure | `.env` / `.env.production` committed to git | accept | Files contain only the public VPS URL `https://andescode.com.ar` — no secrets; `.env.local` stays git-ignored for any future overrides |
| T-01-03 | Tampering | certificates create/update/delete rules | mitigate | Migration sets `createRule/updateRule/deleteRule = null` (superuser only); public clients get 403 on writes |
| T-01-SC | Tampering | npm installs | accept | No new packages installed this plan; `pocketbase@^0.27.0` already in package.json, slopcheck `[OK]` per RESEARCH Package Legitimacy Audit |
</threat_model>

<verification>
- `npm run build` exits 0 (tsc strict + vite build) with augmented `vite-env.d.ts`
- Migration grep confirms exact D-10 API rules and the unique index
- `git check-ignore` confirms `.env` is committed while `.env.local` is ignored
- `deploy/ecosystem.config.js` requires cleanly with `interpreter: "none"` and `--migrationsDir`
</verification>

<success_criteria>
- All four INFRA requirements (04, 05, 06, 08) have a concrete artifact: migration covers 04/05/06, env wiring covers 08
- Migration is reversible and uses `app` callback param
- Frontend type-checks with `VITE_POCKETBASE_URL` typed
- Deploy runbook is complete enough for Plan 02 to run without re-deriving commands
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md` when done
</output>
