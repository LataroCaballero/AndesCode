---
phase: 04-qr-pdf-and-visual-certificate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/assets/logo/andescode-wordmark.png
  - src/assets/logo/fcefn.png
  - src/components/CertificadoVisual.tsx
  - src/sections/CertificadoVerificacion.tsx
autonomous: true
requirements: [VIS-01, QRPDF-01, QRPDF-02]
must_haves:
  truths:
    - "A user visiting /certificados/:certificateCode sees an HTML/CSS reproduction of the physical certificate (logos, name, fields, supervisor block) instead of the old metadata grid"
    - "A QR code is visible inside the certificate visual and encodes the full verification URL (window.location.href)"
    - "The QR uses error correction level H"
    - "A revoked certificate renders the full visual plus a diagonal red REVOCADO watermark; the status badge stays above the fold"
    - "The actions section (status badge + Copiar enlace button) is visible without scrolling on mobile"
  artifacts:
    - path: "src/components/CertificadoVisual.tsx"
      provides: "HTML/CSS certificate reproduction with embedded QR and revoked watermark"
      contains: "QRCodeSVG"
      min_lines: 80
    - path: "src/assets/logo/fcefn.png"
      provides: "FCEFN/UNSJ logo for certificate header"
    - path: "src/assets/logo/andescode-wordmark.png"
      provides: "AndesCode wordmark for certificate header"
  key_links:
    - from: "src/sections/CertificadoVerificacion.tsx"
      to: "src/components/CertificadoVisual.tsx"
      via: "import + <CertificadoVisual cert={cert} />"
      pattern: "CertificadoVisual"
    - from: "src/components/CertificadoVisual.tsx"
      to: "qrcode.react"
      via: "QRCodeSVG with level H"
      pattern: "level=\"H\""
---

<objective>
Deliver the first vertical slice of Phase 4: the public verification page renders a faithful HTML/CSS reproduction of the physical certificate (`ref/assets/certificado.png`) with an embedded, scannable QR code — replacing the Phase 2 metadata grid.

Purpose: Completes VIS-01 (visual certificate) and QRPDF-01/QRPDF-02 (QR on page, level H). After this plan, a real user can SEE the branded certificate and scan the QR with a phone to reach the same verification URL.
Output: New `CertificadoVisual` component, copied logo assets, and a refactored `CertificadoVerificacion` FOUND state (actions section above + certificate visual below, per D-04).
</objective>

<phase_goal>
**As a** person verifying a certificate, **I want to** see the AndesCode certificate rendered visually with a scannable QR code, **so that** I can confirm its authenticity at a glance and re-verify it from any phone.
</phase_goal>

<artifacts_this_phase_produces>
Symbols and paths created or modified in THIS plan:
- `src/components/CertificadoVisual.tsx` — NEW. Default export `CertificadoVisual({ cert }: CertificadoVisualProps)`. Props type `CertificadoVisualProps = { cert: Certificate }`.
- `src/assets/logo/fcefn.png` — NEW asset (copied from `ref/assets/logo fcefn.png`).
- `src/assets/logo/andescode-wordmark.png` — NEW asset (copied from `ref/assets/Logotipo.png`).
- `src/sections/CertificadoVerificacion.tsx` — MODIFIED. FOUND state refactored: actions section + `<CertificadoVisual>`. Adds `FiDownload` to the icon import line (placeholder for Plan 02 wiring) and `max-w-3xl` container.

Note: `src/assets/logo/logo.png` (AndesCode icon) already exists — reuse, do not re-copy.
</artifacts_this_phase_produces>

<execution_context>
@/Users/laucaballero/Desktop/Lautaro/AndesCode/AndesCode/.claude/gsd-core/workflows/execute-plan.md
@/Users/laucaballero/Desktop/Lautaro/AndesCode/AndesCode/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-qr-pdf-and-visual-certificate/04-CONTEXT.md
@.planning/phases/04-qr-pdf-and-visual-certificate/04-UI-SPEC.md
@.planning/phases/04-qr-pdf-and-visual-certificate/04-PATTERNS.md
@ref/assets/certificado.png
@src/sections/CertificadoVerificacion.tsx
@src/types/certificate.ts
@src/components/WelcomeModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy logo assets into src and verify FCEFN alpha channel</name>
  <files>src/assets/logo/fcefn.png, src/assets/logo/andescode-wordmark.png</files>
  <read_first>
    - ref/assets/logo fcefn.png (read as image — inspect for dark vs transparent background, RESEARCH.md Pitfall 7 / Open Question 1)
    - ref/assets/Logotipo.png (the ANDESCODE wordmark)
    - ref/assets/certificado.png (reference design — confirm how each logo appears in the header row)
    - src/assets/logo/logo.png (existing AndesCode icon — already present, do not copy again)
  </read_first>
  <action>
    Copy `ref/assets/Logotipo.png` to `src/assets/logo/andescode-wordmark.png` and `ref/assets/logo fcefn.png` to `src/assets/logo/fcefn.png` (per RESEARCH.md Open Question 2). Use `cp` with exact source/target paths — the source filename `logo fcefn.png` contains a space, quote it. Do NOT copy `ref/assets/logo.png` again — `src/assets/logo/logo.png` already exists as the AndesCode icon.
    Read `ref/assets/logo fcefn.png` as an image and determine whether it has a transparent alpha background or a solid dark/black circular background. Record the finding in the SUMMARY (it drives how the FCEFN logo is placed in Task 2 and in Plan 02's PDF). If the background is solid dark, the certificate header still places it on a white card — note that it may render with a dark circle and that this is acceptable for v1 fidelity to the reference, OR that the AndesCode-only header is the fallback. Do not block; record and proceed.
  </action>
  <verify>
    <automated>test -f src/assets/logo/fcefn.png && test -f src/assets/logo/andescode-wordmark.png && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/assets/logo/fcefn.png` exists (file present, non-zero size)
    - `src/assets/logo/andescode-wordmark.png` exists (file present, non-zero size)
    - `src/assets/logo/logo.png` still exists (was not deleted or overwritten)
    - SUMMARY records whether `fcefn.png` has a transparent or solid dark background
  </acceptance_criteria>
  <done>Both new logo assets exist in src/assets/logo/; FCEFN alpha-channel finding documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create CertificadoVisual component (HTML/CSS reproduction + embedded QR level H)</name>
  <files>src/components/CertificadoVisual.tsx</files>
  <read_first>
    - ref/assets/certificado.png (mandatory — the layout the component must reproduce: header logos, title block, centered student name, description, 4-column field row, supervisor block, QR bottom-right, footer line)
    - .planning/phases/04-qr-pdf-and-visual-certificate/04-UI-SPEC.md (§ Page Layout Contract, § Typography, § Color, § Copywriting Contract, § Reference Certificate Analysis — exact copy strings, sizes, watermark spec)
    - .planning/phases/04-qr-pdf-and-visual-certificate/04-PATTERNS.md (§ CertificadoVisual pattern assignments — imports, props type, white-card container, revoked watermark, QR component, divider, Fira Code footer, formatDate helper, score display)
    - src/components/WelcomeModal.tsx (analog — white card / rounded-xl / border / shadow container pattern, props-type convention)
    - src/sections/CertificadoVerificacion.tsx (source of the formatDate helper and the score conditional-render pattern to copy)
    - src/types/certificate.ts (Certificate interface — all available fields)
  </read_first>
  <action>
    Create `src/components/CertificadoVisual.tsx` as a presentational default-export component `CertificadoVisual({ cert }: CertificadoVisualProps)` with `type CertificadoVisualProps = { cert: Certificate }`. Add the Spanish file-level comment header per PATTERNS.md.
    Import the Certificate type from `../types/certificate.ts`, `QRCodeSVG` from `qrcode.react` (already installed), the logo URLs (`../assets/logo/logo.png`, `../assets/logo/andescode-wordmark.png`, `../assets/logo/fcefn.png`) as plain Vite PNG URL imports (no `?url` needed — vite/client types cover `.png`), and footer-row icons from `react-icons/fi` (e.g. `FiCalendar`, `FiMonitor`, `FiTool`, `FiStar`).
    Build the white-card container: `relative overflow-hidden bg-white border border-[#E5E7EB] rounded-xl p-6 shadow-[0_4px_24px_rgba(0,0,0,0.08)] fade-in` (relative + overflow-hidden are required so the watermark clips to the card). On mobile reduce padding toward 16px per UI-SPEC.
    Reproduce the reference layout top-to-bottom per UI-SPEC § Page Layout Contract: (1) header row with AndesCode icon+wordmark left and FCEFN logo right (`flex-row` even on mobile); (2) `border-t border-[#191919]` divider; (3) title block "CERTIFICADO" (28px/text-2xl bold) + subtitle "DE CULMINACIÓN DE PRÁCTICA PROFESIONAL SITUADA" + italic "se certifica que", all centered; (4) `cert.studentName` centered, all-caps, large (text-3xl/32px bold); (5) `cert.description` paragraph when present; (6) divider; (7) 4-column field row PERÍODO / ÁREA DE DESEMPEÑO / HERRAMIENTAS / CALIFICACIÓN collapsing to `grid-cols-2` on mobile — PERÍODO shows `formatDate(startDate) – formatDate(endDate)`, ÁREA shows `cert.degree`, HERRAMIENTAS shows `cert.technologies` comma-joined, CALIFICACIÓN shows `{cert.score}/100` only when score is present (use the `cert.score !== undefined && cert.score !== null` guard); (8) divider; (9) bottom row: supervisor block (simulated signature line `border-t border-[#191919] pt-1 w-24` + `cert.supervisorName` bold + "Responsable de Gestión de Talento Humano") on the left, AndesCode seal (logo) center, and the QR block right; (10) footer line using `fira-code-regular` showing `// CERTIFICADO: {cert.certificateCode}` left and `San Juan, {formatDate(cert.issueDate)}` right.
    QR block: render `<QRCodeSVG value={window.location.href} size={100} level="H" bgColor="#FFFFFF" fgColor="#191919" marginSize={2} />` with the label "VERIFICÁ LA AUTENTICIDAD" (uppercase, 8px, text-gray-500) below it. `level="H"` is MANDATORY (default is "L"). On mobile the bottom row stacks (`flex-col`) so the QR sits below the supervisor block.
    Revoked watermark: when `cert.status === "revoked"`, render an absolutely-positioned overlay `absolute inset-0 flex items-center justify-center pointer-events-none overflow-hidden` with inner span `text-[#b91c1c] font-bold select-none text-[48px] sm:text-[72px]`, inline style `{ opacity: 0.25, transform: "rotate(-30deg)" }`, text "REVOCADO", and `aria-hidden`. The full certificate still renders beneath it — do not hide or dim any fields.
    Copy the `formatDate` helper into this file (or export it from CertificadoVerificacion and import). Never render `cert.dni` (privacy). Use CSS variables for brand colors and the project's Spanish comment convention. Do not use path aliases.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "CertificadoVisual" || echo "no type errors in CertificadoVisual"</automated>
    <human-check>Open /certificados/:certificateCode for an active cert and a revoked cert; confirm the visual matches ref/assets/certificado.png structure, the QR scans to the same URL, and the REVOCADO watermark appears diagonally on the revoked one.</human-check>
  </verify>
  <acceptance_criteria>
    - `src/components/CertificadoVisual.tsx` exists and default-exports `CertificadoVisual`
    - File contains `QRCodeSVG` with the literal `level="H"`
    - File contains the literal copy strings "CERTIFICADO", "se certifica que", "VERIFICÁ LA AUTENTICIDAD", "Responsable de Gestión de Talento Humano", and "REVOCADO"
    - Watermark block is gated on `cert.status === "revoked"` and uses `pointer-events-none`
    - `cert.dni` does not appear anywhere in the file
    - `cert.score` render is guarded with `!== undefined && !== null`
    - `npm run build` exits 0 (tsc strict + vite build)
  </acceptance_criteria>
  <done>CertificadoVisual renders the full reference layout with a level-H QR and a revoked watermark; build passes strict TypeScript.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor CertificadoVerificacion FOUND state — actions section + CertificadoVisual</name>
  <files>src/sections/CertificadoVerificacion.tsx</files>
  <read_first>
    - src/sections/CertificadoVerificacion.tsx (the file being modified — current FOUND state lines ~153-303, the status badge, handleCopyUrl, and loading skeleton)
    - .planning/phases/04-qr-pdf-and-visual-certificate/04-UI-SPEC.md (§ Page Layout Contract D-04 ordering, § Interaction Contracts button row, § Certificate Visual loading state)
    - .planning/phases/04-qr-pdf-and-visual-certificate/04-PATTERNS.md (§ CertificadoVerificacion modify — preserve list, new imports, button row layout, max-w-3xl change)
    - src/components/CertificadoVisual.tsx (the component being consumed — confirm the `cert` prop signature)
  </read_first>
  <action>
    Refactor only the FOUND state of `CertificadoVerificacion.tsx`. Preserve unchanged: all fetch logic / `useEffect` (lines ~41-91), `FetchState` type, `formatDate`, `handleCopyUrl`, the not-found / error JSX, and the imperative `document.title` behavior.
    Add imports: `import CertificadoVisual from "../components/CertificadoVisual.tsx";` and add `FiDownload` to the existing `react-icons/fi` import line (the actual download button + `usePdfGenerator` wiring lands in Plan 02 — adding `FiDownload` here is fine but it must be used or omitted to satisfy `noUnusedLocals`; if not yet rendered, leave `FiDownload` OUT of this plan and add it in Plan 02 instead to avoid an unused-import build error).
    Change the FOUND container from `max-w-2xl` to `max-w-3xl mx-auto` (UI-SPEC Page Layout Contract). Structure the FOUND state per D-04: (1) the existing status badge block (keep as-is, above the fold, VIS-05); (2) an actions button row `flex flex-col sm:flex-row gap-3 mb-8` containing the existing "Copiar enlace" button (keep `btn-secondary`, `FiCopy`, `min-h 44px`; label "Copiar enlace" per UI-SPEC, success "Enlace copiado!") — leave space for the Plan 02 "Descargar certificado PDF" button; (3) `<CertificadoVisual cert={cert} />` below, separated by the xl (32px) gap.
    Remove the old metadata grid, the standalone student-name `<h1>`, the standalone description block, and the standalone technologies tags — they are now reproduced inside CertificadoVisual (D-01: the visual REPLACES the grid). Update the loading skeleton to the new shape per UI-SPEC § Certificate Visual Loading State: a 48px badge block, a button-row block, then one large rounded-xl ~400px `animate-pulse` block for the certificate card (do not reproduce the internal layout).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
    <human-check>Load an active and a revoked certificate page; confirm status badge + Copiar enlace are above the fold on a 390px screen and the certificate visual renders below.</human-check>
  </verify>
  <acceptance_criteria>
    - `CertificadoVerificacion.tsx` imports and renders `<CertificadoVisual cert={cert} />` in the FOUND state
    - FOUND container uses `max-w-3xl`
    - Old metadata grid (the `grid grid-cols-2 md:grid-cols-3` block), standalone student-name h1, standalone description, and standalone technologies tags are removed from the FOUND state
    - `handleCopyUrl` and the fetch `useEffect` are unchanged
    - No unused imports (no `noUnusedLocals` error)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The FOUND state shows the actions section above and CertificadoVisual below; the old metadata grid is gone; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PocketBase → React (viewRule public) | Certificate fields fetched without auth; already typed as `Certificate` (existing, unchanged) |
| Certificate data → DOM render | Typed fields rendered as React text nodes (auto-escaped by React) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | CertificadoVisual rendering | mitigate | Never render `cert.dni`; acceptance criterion asserts `dni` absent from the file (privacy, inherited from Phase 2) |
| T-04-02 | Tampering | QR value | accept | QR encodes `window.location.href` (the page the user already loaded); no injection vector — value is the browser URL, not user-supplied data |
| T-04-03 | Spoofing | Revoked certificate appearance | mitigate | Revoked certs render the REVOCADO watermark AND keep the red status badge above the fold (D-03); status comes from the typed `cert.status` field, not client state |
</threat_model>

<verification>
- `npm run build` exits 0 (tsc strict mode + vite build) after all three tasks
- `grep -q 'level="H"' src/components/CertificadoVisual.tsx` (QRPDF-02)
- `grep -q 'CertificadoVisual' src/sections/CertificadoVerificacion.tsx` (VIS-01 wired)
- `grep -L 'cert.dni' src/components/CertificadoVisual.tsx` returns the path (dni NOT present)
- Manual (end-of-phase human verify): QR scans to the verification URL on a phone; revoked watermark renders diagonally; status badge above fold on mobile
</verification>

<success_criteria>
- VIS-01: /certificados/:certificateCode renders an HTML/CSS reproduction of the physical certificate
- QRPDF-01: a QR encoding the full verification URL is visible on the page
- QRPDF-02: the QR uses error correction level H
- The metadata grid is fully replaced by the certificate visual (D-01)
- Actions (status badge + Copiar enlace) remain above the fold on mobile (D-04, VIS-05)
</success_criteria>

<output>
Create `.planning/phases/04-qr-pdf-and-visual-certificate/04-01-SUMMARY.md` when done. Record the FCEFN logo alpha-channel finding from Task 1 (consumed by Plan 02's PDF logo embedding).
</output>
