# .env
# Desarrollo — apunta al VPS de producción por D-09 (no hay entorno local de PocketBase)
# ACEPTABLE: URLs públicas de infraestructura (sin tokens, sin contraseñas, sin claves privadas)
# NUNCA AGREGAR: *_PASSWORD, *_TOKEN, *_SECRET, *_KEY, POCKETBASE_ADMIN_*
VITE_POCKETBASE_URL=https://andescode.com.ar
